import os from 'os';

// userInfo() -> Gives infromation on current system user 
console.log(os.userInfo()); 

// totalmem() (bytes )
console.log(os.totalmem());

// freemem() -> Returns free memory 
console.log(os.freemem()); 51539607552

// cpus()
console.log(os.cpus());

